---
title: "Brute::Middleware::DefaultCompactionPipeline::Declines"
description: "The floor of a pipeline given nothing to summarise with: what the free layers managed is what the turn gets."
---


```ruby
module Brute::Middleware::DefaultCompactionPipeline
  class Declines
  end
end
```

The floor of a pipeline given nothing to summarise with: what the free layers
managed is what the turn gets.

## Instance Methods

### #call

```ruby
call(env)
```

*Not documented.*

## Defined in

- `lib/brute/middleware/040_default_compaction_pipeline.rb`
