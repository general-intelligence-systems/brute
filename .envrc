use flake

# Keep the checkout usable: gems matching Gemfile.lock, git hooks installed.
# Both are guarded, so this is ~0.2s and silent when there is nothing to do.
# Re-runs whenever these change, which is exactly when the work is needed.
watch_file Gemfile.lock lefthook.yml

bin/setup
