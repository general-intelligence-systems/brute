---
name: prime-intellect
description: Work with Prime Intellect products via the prime CLI and Python SDKs - verifiers environments and the Environments Hub, evaluations (local and hosted), Hosted Training and prime-rl, code sandboxes, Prime Inference, GPU compute (pods and clusters), storage, and tunnels.
---

# Prime Intellect

SCAFFOLD — instruction-only port of prime-agent
`packages/coding-agent/skills/prime-intellect`. Fill-in: copy the upstream
SKILL.md body and `references/` verbatim (no code); see FEATURES.md (S4).

Prime Intellect is an open superintelligence lab building open-source AGI
infrastructure: a platform for RL environments, evaluations, post-training,
inference, and globally distributed GPU compute. The `prime` CLI is the
default way to interface with every product.
