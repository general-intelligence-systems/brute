# anthropic

Brute + the official anthropic gem.

## Run

```
nix run ./examples/anthropic
nix run ./examples anthropic        # via the dispatcher from the repo root
```

Requires ANTHROPIC_API_KEY; override the model with BRUTE_MODEL.
