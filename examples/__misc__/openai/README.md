# openai

Brute + the official openai gem (https://github.com/openai/openai-ruby) —.

## Run

```
nix run ./examples/openai
nix run ./examples openai        # via the dispatcher from the repo root
```

Requires OPENAI_API_KEY; override the model with BRUTE_MODEL.
