# open-router

Brute + open_router_enhanced (https://github.com/estiens/open_router_enhanced).

## Run

```
nix run ./examples/open-router
nix run ./examples open-router        # via the dispatcher from the repo root
```

Requires OPENROUTER_API_KEY; override the model with BRUTE_MODEL.
