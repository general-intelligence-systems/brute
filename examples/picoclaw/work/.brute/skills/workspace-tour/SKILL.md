---
name: workspace-tour
description: Give a tour of this picoclaw-clone workspace — what AGENTS.md, SOUL.md, USER.md, HEARTBEAT.md, memory/, sessions/ and .brute/skills/ are for
---

Give the user a short tour of the workspace. For each of the files below,
use the `read` tool if it exists and summarize what it currently contains in
one line; otherwise note that it is missing:

1. `AGENTS.md` — the agent's behavior guide
2. `SOUL.md` — the agent's identity and style
3. `USER.md` — notes about the user
4. `HEARTBEAT.md` — periodic tasks for scheduled runs
5. `memory/MEMORY.md` — long-term memory
6. `config.json` — model and sandbox settings

Then list `sessions/` (past conversations) and `.brute/skills/` (loadable
skills) and finish with one sentence on how to change the agent's behavior:
edit the workspace files, no restart needed.
